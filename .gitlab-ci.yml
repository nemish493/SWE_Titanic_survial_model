stages:
  - test

e2e_tests:
  image: cypress/base:latest
  stage: test
  script:
    - npm install
    - npx cypress run
  artifacts:
    when: always
    paths:
      - cypress/screenshots
      - cypress/videos
